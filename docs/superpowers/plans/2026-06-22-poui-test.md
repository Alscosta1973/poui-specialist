# poui-test Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar a skill `/poui-specialist:test` que gera `*.component.spec.ts` completo (Karma + Jasmine) para os 13 tipos de componente testáveis do plugin.

**Architecture:** Skill em camadas: `SKILL.md` com 5 passos + `templates-test-base.md` (boilerplate compartilhado) + 5 arquivos de família (`list`, `form`, `detail`, `complex`, `other`). O agente lê o `.component.ts` existente no Passo 2 para identificar a família e derivar placeholders antes de gerar o spec.

**Tech Stack:** Angular 17+, Karma, Jasmine, `provideHttpClient()` + `provideHttpClientTesting()`, `HttpTestingController`, `provideRouter([])`, `fakeAsync`/`tick`.

## Global Constraints

- Standalone components: imports de componente em `imports:[]` do TestBed, nunca em `declarations:[]`
- `ChangeDetectionStrategy.OnPush`: chamar `fixture.detectChanges()` explicitamente após flush HTTP
- Angular 17+ pattern: `provideHttpClient()` + `provideHttpClientTesting()` em vez de `HttpClientTestingModule`
- `provideRouter([])` em vez de `RouterTestingModule`
- Placeholders: `{{ComponentClass}}`, `{{kebab-name}}`, `{{ServiceClass}}`, `{{serviceFile}}`, `{{apiPath}}`, `{{ModelInterface}}`, `{{moduleName}}`
- Todos os novos arquivos do plugin em `skills/poui-test/`
- Commits: conventional commits — `feat(skill):`, `feat(template):`, `feat(command):`
- Nunca adicionar Claude como co-autor nos commits
- **Sem arquivo de agente:** o spec mencionou "registrar agente" mas `/test` segue o padrão de `/quality` e `/build-fix` — skill invocada diretamente pelo command, sem subagente. Não criar `agents/test-generator.md`.

---

### Task 1: SKILL.md + commands/test.md

**Files:**
- Create: `skills/poui-test/SKILL.md`
- Create: `commands/test.md`

**Interfaces:**
- Produces: skill `poui-test` invocável via `/poui-specialist:test <ComponentClass> --module <module>`; 5 passos definidos; mapeamento tipo→família documentado

- [ ] **Step 1: Criar `skills/poui-test/SKILL.md`**

Escrever o arquivo com o conteúdo abaixo:

```markdown
---
name: poui-test
description: Generates Angular unit test spec files (Karma + Jasmine) for components created by poui-specialist — smoke, loading, HTTP, router, modals | © Andre Costa — uso restrito · https://github.com/Alscosta1973/poui-specialist
---

# PO-UI Test Generator

Gera `*.component.spec.ts` completo (Karma + Jasmine) para componentes gerados pelo plugin, cobrindo: smoke test, estados de loading/empty/error, chamadas HTTP via `HttpTestingController`, navegação via `Router`, e interações específicas por tipo.

## Uso

```
/poui-specialist:test <ComponentClass> --module <module>
```

**Exemplos:**
```
/poui-specialist:test ParceirosComponent --module faturamento/parceiros
/poui-specialist:test TitulosListComponent --module financeiro
/poui-specialist:test TitulosService --module financeiro
```

---

## Passo 1 — Parse argumentos

Extrair `ComponentClass` e `--module`.

Derivar:
- `kebab-name` — PascalCase → kebab-case (`TitulosListComponent` → `titulos-list`)
- `isService` — verdadeiro se `ComponentClass` terminar em `Service`
- `componentPath`:
  - se `isService`: `src/app/<module>/<kebab-name>.service.ts`
  - senão: `src/app/<module>/<kebab-name>/<kebab-name>.component.ts`
- `specPath`: mesmo caminho mas com `.spec.ts`

Se o arquivo não existir:
```
⚠ Arquivo não encontrado: <componentPath>
   Verifique ComponentClass e --module e tente novamente.
```
Encerrar.

---

## Passo 2 — Ler arquivo e identificar família

Ler o arquivo `.component.ts` (ou `.service.ts`). Ler também o `.service.ts` associado para obter `apiPath`.

**Extrair:**
- `ServiceClass` — via `inject(<ServiceClass>)` no componente
- `serviceFile` — do import path (ex: `'../titulos.service'` → `titulos.service`)
- `apiPath` — `readonly apiUrl` ou `readonly apiUrl` no service
- `ModelInterface` — interface usada em `items: signal<ModelInterface[]>([])`

**Mapeamento tipo → família:**

| Indicadores no arquivo | Família |
|------------------------|---------|
| `@Injectable` (é serviço) | `other` |
| `PoChartModule` ou `PoWidgetModule` | `other` |
| `actions: ActionConfig[]` + `currentAction` + `actionLoading` | `complex` |
| `selectedLeft` + `selectedRight` | `complex` |
| `activeBrowse` | `complex` |
| `detailColumns` ou `p-detail-columns` | `complex` |
| `PoStepperModule` ou `currentStep` | `form` |
| `PoDynamicFormModule` + `ActivatedRoute` | `form` |
| `PoDynamicFormModule` sem `ActivatedRoute` | `form` |
| `ActivatedRoute` sem form | `detail` |
| `PoDisclaimerGroup` | `list` |
| `PoPageDynamicTableComponent` | `list` |
| `items` + `loading` + `hasNext` | `list` |

---

## Passo 3 — Carregar templates

Ler em sequência:
1. `skills/poui-test/templates-test-base.md` — exceto se família `other/service` (tem setup próprio)
2. `skills/poui-test/templates-test-<família>.md`

---

## Passo 4 — Gerar spec

Substituir todos os `{{placeholder}}` pelos valores derivados no Passo 2.

Preencher dados mock para `{{ModelInterface}}` baseado nos campos vistos nas `columns: PoTableColumn[]` e nos imports do modelo.

Substituir comentários `// Agente:` pelos métodos reais encontrados no `.component.ts`.

Escrever resultado em `<specPath>`.

Confirmar:
```
✅ Spec gerado: <specPath>
```

---

## Passo 5 — Executar e reportar

```powershell
ng test --include="<specPath>" --watch=false
```

Exibir:
```
✅ <ComponentClass> — N spec(s), 0 failures
```

Se falhar: exibir erros completos. O spec foi gerado e pode ser ajustado manualmente.
```

- [ ] **Step 2: Criar `commands/test.md`**

```markdown
---
description: Generate Karma + Jasmine unit tests for PO-UI Angular components
allowed-tools: Read, Write, Glob, Grep, Skill, Bash
argument-hint: "<ComponentClass> --module <module>"
---

**IMPORTANT:** Always respond in the same language the user is writing in.

# /poui-specialist:test

Gera `*.component.spec.ts` completo (Karma + Jasmine) para um componente gerado pelo plugin, cobrindo smoke test, HTTP, router e interações específicas por tipo.

## Exemplos

```bash
/poui-specialist:test ParceirosComponent --module faturamento/parceiros
/poui-specialist:test TitulosListComponent --module financeiro
/poui-specialist:test TitulosService --module financeiro
```

## Processo

1. **Invocar skill `poui-specialist:poui-test`** — executa os 5 passos: parse de argumentos, leitura do componente, seleção de template por família, geração do spec com dados mock realistas, execução de `ng test`.
```

- [ ] **Step 3: Commitar**

```bash
git add skills/poui-test/SKILL.md commands/test.md
git commit -m "feat(skill): adicionar poui-test — skeleton SKILL.md e commands/test.md"
```

Resultado esperado: `[master <hash>] feat(skill): adicionar poui-test — skeleton SKILL.md e commands/test.md`

---

### Task 2: templates-test-base.md

**Files:**
- Create: `skills/poui-test/templates-test-base.md`

**Interfaces:**
- Produces: boilerplate de TestBed (imports, setup, afterEach) prefixado a todos os templates de família exceto `other/service`

- [ ] **Step 1: Criar `skills/poui-test/templates-test-base.md`**

```markdown
# Test Base — Boilerplate Compartilhado

Prefixado a todos os specs exceto `other/service` (que tem setup próprio).
O agente insere este bloco antes dos cenários do template de família.

## Imports e setup

```typescript
import { ComponentFixture, TestBed, fakeAsync, tick } from '@angular/core/testing';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting, HttpTestingController } from '@angular/common/http/testing';
import { provideRouter } from '@angular/router';
import { PoNotificationService } from '@po-ui/ng-components';
import { {{ComponentClass}} } from './{{kebab-name}}.component';

describe('{{ComponentClass}}', () => {
  let component: {{ComponentClass}};
  let fixture: ComponentFixture<{{ComponentClass}}>;
  let httpMock: HttpTestingController;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [{{ComponentClass}}],
      providers: [
        provideHttpClient(),
        provideHttpClientTesting(),
        provideRouter([]),
      ],
    }).compileComponents();

    fixture = TestBed.createComponent({{ComponentClass}});
    component = fixture.componentInstance;
    httpMock = TestBed.inject(HttpTestingController);
  });

  afterEach(() => {
    httpMock.verify();
  });

  // ── cenários do template de família abaixo ────────────────────────────────
```

## Notas

- `OnPush`: chamar `fixture.detectChanges()` após qualquer flush HTTP
- `fakeAsync` + `tick()` para observables RxJS e `takeUntilDestroyed`
- `httpMock.verify()` no `afterEach` garante que nenhuma request ficou sem flush
- Fechar o bloco `describe` com `});` na última linha do template de família
```

- [ ] **Step 2: Commitar**

```bash
git add skills/poui-test/templates-test-base.md
git commit -m "feat(template): adicionar templates-test-base — boilerplate TestBed + HttpTestingController"
```

---

### Task 3: templates-test-list.md + validação com ParceirosComponent

**Files:**
- Create: `skills/poui-test/templates-test-list.md`
- Create (validation artifact): `Teste_poui/src/app/faturamento/parceiros/parceiros.component.spec.ts`

**Interfaces:**
- Consumes: `templates-test-base.md`; `ParceirosComponent` em `Teste_poui/src/app/faturamento/parceiros/`
- Produces: template para page-list / page-dynamic-search / page-dynamic; spec de parceiros passando em `ng test`

- [ ] **Step 1: Criar `skills/poui-test/templates-test-list.md`**

```markdown
# Test Template — Família List

Cobre: `page-list`, `page-dynamic-search`, `page-dynamic`

Inserir após o bloco base. Fechar com `});` na última linha.

## Cenários

```typescript
  // ── dados mock ──────────────────────────────────────────────────────────
  // Agente: preencher campos baseado nos `columns: PoTableColumn[]` do componente
  const mockItem: {{ModelInterface}} = {
  } as {{ModelInterface}};

  const mockResponse = { items: [mockItem], hasNext: false };

  // ── smoke ────────────────────────────────────────────────────────────────
  it('should create', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockResponse));
    tick();
    fixture.detectChanges();
    expect(component).toBeTruthy();
  }));

  // ── load inicial (GET page=1) ─────────────────────────────────────────────
  it('should load items on init', fakeAsync(() => {
    fixture.detectChanges();
    const req = httpMock.expectOne(r => r.url.includes('{{apiPath}}'));
    expect(req.request.method).toBe('GET');
    expect(req.request.params.get('page')).toBe('1');
    req.flush(mockResponse);
    tick();
    fixture.detectChanges();
    expect(component.items()).toEqual([mockItem]);
    expect(component.loading()).toBeFalse();
  }));

  // ── loading signal ───────────────────────────────────────────────────────
  it('should set loading true during request and false after', fakeAsync(() => {
    fixture.detectChanges();
    expect(component.loading()).toBeTrue();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();
    fixture.detectChanges();
    expect(component.loading()).toBeFalse();
  }));

  // ── empty state ──────────────────────────────────────────────────────────
  it('should handle empty response', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush({ items: [], hasNext: false });
    tick();
    fixture.detectChanges();
    expect(component.items()).toEqual([]);
    expect(component.hasNext()).toBeFalse();
  }));

  // ── error state ──────────────────────────────────────────────────────────
  it('should call notification.error on HTTP failure', fakeAsync(() => {
    const notifSpy = spyOn(TestBed.inject(PoNotificationService), 'error');
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(
      'Server error', { status: 500, statusText: 'Internal Server Error' }
    );
    tick();
    fixture.detectChanges();
    expect(notifSpy).toHaveBeenCalled();
    expect(component.loading()).toBeFalse();
  }));

  // ── filtro / quick search ────────────────────────────────────────────────
  it('should GET with q param on quick search', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();
    // Agente: substituir pelo método de busca real (ex: onQuickSearch, filterSettings.action)
    component.onQuickSearch('teste');
    fixture.detectChanges();
    const req = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}') && r.params.get('q') === 'teste'
    );
    expect(req.request.method).toBe('GET');
    req.flush({ items: [], hasNext: false });
    tick();
    fixture.detectChanges();
    expect(component.items()).toEqual([]);
  }));

  // ── show more ───────────────────────────────────────────────────────────
  it('should GET page=2 on show more', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush({ items: [mockItem], hasNext: true });
    tick();
    fixture.detectChanges();
    expect(component.hasNext()).toBeTrue();

    // Agente: substituir pelo método show-more real (ex: onShowMore)
    component.onShowMore();
    fixture.detectChanges();
    const req = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}') && r.params.get('page') === '2'
    );
    req.flush({ items: [mockItem], hasNext: false });
    tick();
    fixture.detectChanges();
    expect(component.items().length).toBe(2);
    expect(component.hasNext()).toBeFalse();
  }));

  // ── page-dynamic-search: filtro avançado ─────────────────────────────────
  // Agente: incluir apenas se PoDisclaimerGroup presente no componente
  it('should GET with filter params on advanced search', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();
    // Agente: invocar o método de filtro avançado real com campos reais do modelo
    // component.onAdvancedSearch({ campo: 'valor' });
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r =>
      r.flush({ items: [], hasNext: false })
    );
    tick();
    fixture.detectChanges();
    expect(component.items()).toEqual([]);
  }));
});
```

## Nota para o agente

- Substituir comentários `// Agente:` pelo método real encontrado no `.component.ts`
- Para `page-dynamic` (PoPageDynamicTableComponent), verificar se o componente gerencia `items` via signal próprio ou delega ao PoPageDynamicTable — adaptar cenários conforme
- Preencher `mockItem` com campos realistas baseados nos `labels` de `columns: PoTableColumn[]`
```

- [ ] **Step 2: Commitar o template**

```bash
git add skills/poui-test/templates-test-list.md
git commit -m "feat(template): adicionar templates-test-list — page-list/dynamic-search/dynamic"
```

- [ ] **Step 3: Validar — invocar `/test` no ParceirosComponent**

Invocar a skill:
```
/poui-specialist:test ParceirosComponent --module faturamento/parceiros
```

O agente deve:
1. Localizar `Teste_poui/src/app/faturamento/parceiros/parceiros.component.ts` ✓
2. Identificar família: `list` (PoDisclaimerGroup → page-dynamic-search)
3. Gerar `Teste_poui/src/app/faturamento/parceiros/parceiros.component.spec.ts`

- [ ] **Step 4: Executar ng test**

```powershell
cd "C:\TOTVS\Projetos\Claude\poui-specialist\Teste_poui"
ng test --include="src/app/faturamento/parceiros/parceiros.component.spec.ts" --watch=false
```

Resultado esperado:
```
Executed N of N SUCCESS
```

Se falhar: corrigir `templates-test-list.md` conforme erros, regenerar o spec, repetir. Não avançar com falhas.

- [ ] **Step 5: Commitar spec de validação**

```bash
git add Teste_poui/src/app/faturamento/parceiros/parceiros.component.spec.ts
git commit -m "feat(teste_poui): adicionar spec ParceirosComponent — validacao template test-list"
```

---

### Task 4: templates-test-form.md + templates-test-detail.md

**Files:**
- Create: `skills/poui-test/templates-test-form.md`
- Create: `skills/poui-test/templates-test-detail.md`

**Interfaces:**
- Consumes: `templates-test-base.md`
- Produces: templates para page-edit / modal-crud / stepper-form e page-detail

> **Nota:** Não há fixture de form/detail em Teste_poui. A validação manual é feita por inspeção do spec gerado + `ng build` (sem `ng test`) para confirmar ausência de erros TypeScript. A execução completa de `ng test` fica disponível quando o usuário gerar um componente form/detail via `/generate`.

- [ ] **Step 1: Criar `skills/poui-test/templates-test-form.md`**

```markdown
# Test Template — Família Form

Cobre: `page-edit`, `modal-crud`, `stepper-form`

Inserir após o bloco base. Fechar com `});` na última linha.

## Cenários

```typescript
  // ── dados mock ──────────────────────────────────────────────────────────
  // Agente: preencher campos baseado nos `fields: PoDynamicFormField[]` do componente
  const mockItem: {{ModelInterface}} = {
  } as {{ModelInterface}};

  // ── smoke ────────────────────────────────────────────────────────────────
  it('should create', fakeAsync(() => {
    fixture.detectChanges();
    // Agente: flush GET inicial se page-edit (carrega por :id da rota)
    // Para modal-crud: flush GET da lista
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockItem));
    tick();
    fixture.detectChanges();
    expect(component).toBeTruthy();
  }));

  // ── carregamento por route param (page-edit / stepper-form) ──────────────
  // Agente: incluir apenas se ActivatedRoute com :id estiver presente
  it('should load item by route param on init', fakeAsync(() => {
    fixture.detectChanges();
    const req = httpMock.expectOne(r => r.url.match(new RegExp('{{apiPath}}/.+')));
    expect(req.request.method).toBe('GET');
    req.flush(mockItem);
    tick();
    fixture.detectChanges();
    expect(component.loading()).toBeFalse();
  }));

  // ── submit → POST (novo registro) ────────────────────────────────────────
  it('should call POST on save new item', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockItem));
    tick();
    fixture.detectChanges();

    // Agente: invocar método de save real (ex: onSave, onSubmit)
    component.onSave(mockItem);
    fixture.detectChanges();

    const req = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}') && r.method === 'POST'
    );
    expect(req.request.body).toEqual(jasmine.objectContaining(mockItem));
    req.flush(mockItem);
    tick();
    fixture.detectChanges();
    // Agente: para page-edit verificar router.navigate; para modal-crud verificar modal fechado
  }));

  // ── submit → PUT (edição) ─────────────────────────────────────────────────
  it('should call PUT on update existing item', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockItem));
    tick();
    fixture.detectChanges();

    // Agente: invocar com item existente (com ID/chave preenchida)
    component.onSave(mockItem);
    fixture.detectChanges();

    const req = httpMock.expectOne(r =>
      r.url.match(new RegExp('{{apiPath}}/.+')) && r.method === 'PUT'
    );
    req.flush(mockItem);
    tick();
    fixture.detectChanges();
    expect(component.loading()).toBeFalse();
  }));

  // ── erro no submit ────────────────────────────────────────────────────────
  it('should show error notification on save failure', fakeAsync(() => {
    const notifSpy = spyOn(TestBed.inject(PoNotificationService), 'error');
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockItem));
    tick();

    component.onSave(mockItem);
    fixture.detectChanges();

    httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}') && (r.method === 'POST' || r.method === 'PUT')
    ).flush('Error', { status: 400, statusText: 'Bad Request' });
    tick();
    fixture.detectChanges();

    expect(notifSpy).toHaveBeenCalled();
    expect(component.loading()).toBeFalse();
  }));

  // ── modal-crud: abrir modal para novo registro ───────────────────────────
  // Agente: incluir apenas se for modal-crud (lista com modal inline)
  it('should open modal on new item action', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush({ items: [mockItem], hasNext: false });
    tick();
    fixture.detectChanges();

    // Agente: invocar método que abre o modal (ex: openModal, onNew)
    // component.openModal();
    fixture.detectChanges();
    // Agente: verificar signal ou propriedade de estado do modal
    // expect(component.isModalOpen()).toBeTrue();
  }));
});
```

## Nota para o agente

- Substituir comentários `// Agente:` pelo método e signal reais do `.component.ts`
- Para `stepper-form`: adaptar cenários de submit para "avançar steps" + submit no último step
- Para `modal-crud`: o componente gerencia lista (`items`) + modal state no mesmo arquivo; adaptar flush do load inicial para `{ items: [...], hasNext: false }`
- Não incluir cenário de `ActivatedRoute` em modal-crud (não usa rota com `:id`)
```

- [ ] **Step 2: Criar `skills/poui-test/templates-test-detail.md`**

```markdown
# Test Template — Família Detail

Cobre: `page-detail`

Inserir após o bloco base. Fechar com `});` na última linha.

## Cenários

```typescript
  // ── dados mock ──────────────────────────────────────────────────────────
  // Agente: preencher campos baseado nos campos exibidos no template HTML
  const mockItem: {{ModelInterface}} = {
  } as {{ModelInterface}};

  // ── smoke ────────────────────────────────────────────────────────────────
  it('should create', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.match(new RegExp('{{apiPath}}/.+'))).flush(mockItem);
    tick();
    fixture.detectChanges();
    expect(component).toBeTruthy();
  }));

  // ── carrega item por route param ──────────────────────────────────────────
  it('should load item on init via route param', fakeAsync(() => {
    fixture.detectChanges();
    const req = httpMock.expectOne(r => r.url.match(new RegExp('{{apiPath}}/.+')));
    expect(req.request.method).toBe('GET');
    req.flush(mockItem);
    tick();
    fixture.detectChanges();
    expect(component.loading()).toBeFalse();
  }));

  // ── botão Editar → router.navigate ───────────────────────────────────────
  it('should navigate to edit route on edit action', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.match(new RegExp('{{apiPath}}/.+'))).flush(mockItem);
    tick();
    fixture.detectChanges();

    const router = TestBed.inject(Router);
    const navigateSpy = spyOn(router, 'navigate');

    // Agente: invocar método de edição real (ex: onEdit, ação de page actions)
    // component.onEdit();
    expect(navigateSpy).toHaveBeenCalledWith(jasmine.arrayContaining(['edit']));
  }));

  // ── botão Excluir → DELETE → router.navigate back ────────────────────────
  it('should call DELETE and navigate back on delete', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.match(new RegExp('{{apiPath}}/.+'))).flush(mockItem);
    tick();
    fixture.detectChanges();

    const router = TestBed.inject(Router);
    const navigateSpy = spyOn(router, 'navigate');

    // Agente: invocar método de exclusão real
    // component.onDelete();
    fixture.detectChanges();

    httpMock.expectOne(r =>
      r.url.match(new RegExp('{{apiPath}}/.+')) && r.method === 'DELETE'
    ).flush(null, { status: 204, statusText: 'No Content' });
    tick();
    fixture.detectChanges();
    expect(navigateSpy).toHaveBeenCalled();
  }));
});
```

## Nota para o agente

- Adicionar `import { Router } from '@angular/router';` ao bloco de imports no topo do spec
- Para testes que dependem do `:id` da rota, verificar se o componente usa `ActivatedRoute` via `inject` ou via `snapshot.params` — adaptar o mock de rota conforme necessário
- Substituir comentários `// Agente:` pelo método real encontrado no `.component.ts`
```

- [ ] **Step 3: Commitar templates**

```bash
git add skills/poui-test/templates-test-form.md skills/poui-test/templates-test-detail.md
git commit -m "feat(template): adicionar templates-test-form e templates-test-detail"
```

- [ ] **Step 4: Verificar compilação TypeScript dos templates**

Inspecionar os templates recém-criados e confirmar que o TypeScript gerado pelos placeholders não tem erros estruturais óbvios (parênteses balanceados, imports completos, `describe`/`it` fechados).

```powershell
# Verificar que o build do projeto base continua passando (não gera specs ainda)
cd "C:\TOTVS\Projetos\Claude\poui-specialist\Teste_poui"
ng build --configuration development 2>&1 | Select-Object -Last 5
```

Resultado esperado: `Application bundle generation complete. [N seconds]` sem erros.

---

### Task 5: templates-test-complex.md + validação com TitulosListComponent

**Files:**
- Create: `skills/poui-test/templates-test-complex.md`
- Create (validation artifact): `Teste_poui/src/app/financeiro/titulos-list/titulos-list.component.spec.ts`

**Interfaces:**
- Consumes: `templates-test-base.md`; `TitulosListComponent` + `TitulosService` em Teste_poui
- Produces: template para action-list / two-panel-browse / stacked-browse / master-detail; spec de titulos passando em `ng test`

- [ ] **Step 1: Criar `skills/poui-test/templates-test-complex.md`**

```markdown
# Test Template — Família Complex

Cobre: `action-list`, `two-panel-browse`, `stacked-browse`, `master-detail`

Inserir após o bloco base. O agente seleciona as seções relevantes ao subtipo identificado no Passo 2.

## Cenários base (todos os subtipos)

```typescript
  // ── dados mock ──────────────────────────────────────────────────────────
  // Agente: preencher campos baseado nos columns: PoTableColumn[] do componente
  const mockItem: {{ModelInterface}} = {
  } as {{ModelInterface}};

  const mockResponse = { items: [mockItem], hasNext: false };

  // ── smoke ────────────────────────────────────────────────────────────────
  it('should create', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockResponse));
    tick();
    fixture.detectChanges();
    expect(component).toBeTruthy();
  }));

  // ── load inicial ─────────────────────────────────────────────────────────
  it('should load items on init', fakeAsync(() => {
    fixture.detectChanges();
    const req = httpMock.expectOne(r => r.url.includes('{{apiPath}}'));
    expect(req.request.method).toBe('GET');
    req.flush(mockResponse);
    tick();
    fixture.detectChanges();
    expect(component.items()).toContain(mockItem);
    expect(component.loading()).toBeFalse();
  }));
```

## Cenários action-list (incluir se `actions: ActionConfig[]` presente)

```typescript
  // ── seleção / desseleção de linha ─────────────────────────────────────────
  it('should update selectedRows on row select/unselect', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();
    fixture.detectChanges();

    component.onRowSelected(mockItem);
    expect(component.selectedRows()).toContain(mockItem);

    component.onRowUnselected(mockItem);
    expect(component.selectedRows()).not.toContain(mockItem);
  }));

  // ── abrir modal de confirmação ────────────────────────────────────────────
  it('should set currentAction on openAction', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();
    fixture.detectChanges();

    // Agente: usar component.actions[0] ou o primeiro ActionConfig real
    const firstAction = component.actions[0];
    component.openAction(firstAction, [mockItem]);
    fixture.detectChanges();

    expect(component.currentAction()).not.toBeNull();
    expect(component.currentAction()?.config.id).toBe(firstAction.id);
  }));

  // ── executar ação → POST → sucesso total → notification.success ──────────
  it('should notify success and clear action on full success response', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();

    const notifSpy = spyOn(TestBed.inject(PoNotificationService), 'success');
    const firstAction = component.actions[0];
    component.openAction(firstAction, [mockItem]);
    component.executeAction();
    fixture.detectChanges();

    // Flush POST da ação
    httpMock.expectOne(r => r.method === 'POST').flush({ sucesso: 1, falha: 0, itens: [] });
    tick();

    // Flush reload da lista após a ação
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockResponse));
    tick();
    fixture.detectChanges();

    expect(notifSpy).toHaveBeenCalled();
    expect(component.currentAction()).toBeNull();
  }));

  // ── executar ação → POST → falha parcial → actionResults preenchido ───────
  it('should set actionResults on partial failure response', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();

    const firstAction = component.actions[0];
    component.openAction(firstAction, [mockItem]);
    component.executeAction();
    fixture.detectChanges();

    // Agente: substituir `mockItem['numero']` pelo campo chave real do modelo
    httpMock.expectOne(r => r.method === 'POST').flush({
      sucesso: 0,
      falha: 1,
      itens: [{ id: String(mockItem['numero' as keyof {{ModelInterface}}]), status: 'erro', mensagem: 'Erro simulado' }],
    });
    tick();

    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockResponse));
    tick();
    fixture.detectChanges();

    expect(component.actionResults()).not.toBeNull();
    expect(component.actionResults()?.falha).toBe(1);
  }));

  // ── executar ação → erro HTTP → notification.error ──────────────────────
  it('should call notification.error on action HTTP failure', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();

    const notifSpy = spyOn(TestBed.inject(PoNotificationService), 'error');
    const firstAction = component.actions[0];
    component.openAction(firstAction, [mockItem]);
    component.executeAction();
    fixture.detectChanges();

    httpMock.expectOne(r => r.method === 'POST').flush(
      'Server error', { status: 500, statusText: 'Internal Server Error' }
    );
    tick();
    fixture.detectChanges();

    expect(notifSpy).toHaveBeenCalled();
    expect(component.currentAction()).toBeNull();
  }));
```

## Cenários two-panel-browse (incluir se `selectedLeft` + `selectedRight` presentes)

```typescript
  it('should enable confirm when both panels have selection', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockResponse));
    tick();
    fixture.detectChanges();

    // Agente: invocar métodos reais de seleção por painel
    // component.onSelectLeft(mockItem);
    // component.onSelectRight(mockItem);
    fixture.detectChanges();
    // Agente: verificar signal ou computed que controla o botão de confirmação
    // expect(component.canConfirm()).toBeTrue();
  }));
```

## Cenários stacked-browse (incluir se `activeBrowse` presente)

```typescript
  it('should switch activeBrowse on tab', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockResponse));
    tick();
    fixture.detectChanges();

    const initialBrowse = component.activeBrowse();
    // Agente: invocar método de troca de browse real (ex: onTabKey, switchBrowse)
    // component.onTabKey();
    fixture.detectChanges();
    expect(component.activeBrowse()).not.toBe(initialBrowse);
  }));
```

## Cenários master-detail (incluir se `detailColumns` presente)

```typescript
  it('should load detail rows on row expand', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(mockResponse);
    tick();
    fixture.detectChanges();

    // Agente: invocar método de expansão real com mockItem
    // component.onRowExpand(mockItem);
    fixture.detectChanges();

    // Agente: ajustar URL do detalhe conforme endpoint real
    const detailReq = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}') && r.url.includes('/itens')
    );
    detailReq.flush({ items: [], hasNext: false });
    tick();
    fixture.detectChanges();
  }));
```

```
Fechar com:
```
});
```

## Nota para o agente

- Substituir comentários `// Agente:` e campos com `mockItem['campo']` pelos valores reais do modelo
- Incluir APENAS os blocos de cenários correspondentes ao subtipo identificado no Passo 2
- Para action-list: `string(mockItem['numero' as keyof Titulo])` — usar o campo `campoChave` real de `actions[0].campoChave`
```

- [ ] **Step 2: Commitar o template**

```bash
git add skills/poui-test/templates-test-complex.md
git commit -m "feat(template): adicionar templates-test-complex — action-list, two-panel-browse, stacked-browse, master-detail"
```

- [ ] **Step 3: Validar — invocar `/test` no TitulosListComponent**

Invocar:
```
/poui-specialist:test TitulosListComponent --module financeiro
```

O agente deve:
1. Localizar `Teste_poui/src/app/financeiro/titulos-list/titulos-list.component.ts`
2. Identificar família: `complex` (action-list — `actions: ActionConfig[]` + `currentAction` + `actionLoading`)
3. Ler `TitulosService` → `apiPath = '/rest/api/custom/v1/financeiro/titulos'`
4. Preencher `mockItem` com campos reais de `Titulo` (numero, parceiro, valor, venc, situacao)
5. Gerar spec em `Teste_poui/src/app/financeiro/titulos-list/titulos-list.component.spec.ts`

- [ ] **Step 4: Executar ng test**

```powershell
cd "C:\TOTVS\Projetos\Claude\poui-specialist\Teste_poui"
ng test --include="src/app/financeiro/titulos-list/titulos-list.component.spec.ts" --watch=false
```

Resultado esperado:
```
Executed N of N SUCCESS
```

Se falhar: corrigir `templates-test-complex.md` conforme erros, regenerar o spec, repetir. Não avançar com falhas.

- [ ] **Step 5: Commitar spec de validação**

```bash
git add Teste_poui/src/app/financeiro/titulos-list/titulos-list.component.spec.ts
git commit -m "feat(teste_poui): adicionar spec TitulosListComponent — validacao template test-complex"
```

---

### Task 6: templates-test-other.md + integração + push

**Files:**
- Create: `skills/poui-test/templates-test-other.md`
- Modify: `commands/generate.md`

**Interfaces:**
- Consumes: `TitulosService` em Teste_poui (validação de service)
- Produces: template para dashboard/service; `generate.md` atualizado com referência ao `/test`; push para `origin`

- [ ] **Step 1: Criar `skills/poui-test/templates-test-other.md`**

```markdown
# Test Template — Família Other

Cobre: `dashboard`, `service`

## Template dashboard

Inserir após o bloco base. Fechar com `});` na última linha.

```typescript
  // ── dados mock ──────────────────────────────────────────────────────────
  // Agente: derivar campos do response GET de KPIs visto no .component.ts
  const mockKpis = {
  };

  // ── smoke ────────────────────────────────────────────────────────────────
  it('should create', fakeAsync(() => {
    fixture.detectChanges();
    httpMock.match(r => r.url.includes('{{apiPath}}')).forEach(r => r.flush(mockKpis));
    tick();
    fixture.detectChanges();
    expect(component).toBeTruthy();
  }));

  // ── carrega KPIs e atualiza signals ──────────────────────────────────────
  it('should load KPIs on init', fakeAsync(() => {
    fixture.detectChanges();
    const req = httpMock.expectOne(r => r.url.includes('{{apiPath}}'));
    expect(req.request.method).toBe('GET');
    req.flush(mockKpis);
    tick();
    fixture.detectChanges();
    expect(component.loading()).toBeFalse();
    // Agente: verificar signal de KPI real (ex: component.kpis(), component.totalVendas())
  }));

  // ── error state ──────────────────────────────────────────────────────────
  it('should handle HTTP error on KPI load', fakeAsync(() => {
    const notifSpy = spyOn(TestBed.inject(PoNotificationService), 'error');
    fixture.detectChanges();
    httpMock.expectOne(r => r.url.includes('{{apiPath}}')).flush(
      'Error', { status: 500, statusText: 'Server Error' }
    );
    tick();
    fixture.detectChanges();
    expect(component.loading()).toBeFalse();
    expect(notifSpy).toHaveBeenCalled();
  }));
});
```

---

## Template service

Para services o agente usa este template independente (ignora `templates-test-base.md`).

```typescript
import { TestBed } from '@angular/core/testing';
import { provideHttpClient } from '@angular/common/http';
import { provideHttpClientTesting, HttpTestingController } from '@angular/common/http/testing';
import { {{ServiceClass}} } from './{{serviceFile}}';

describe('{{ServiceClass}}', () => {
  let service: {{ServiceClass}};
  let httpMock: HttpTestingController;

  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [
        {{ServiceClass}},
        provideHttpClient(),
        provideHttpClientTesting(),
      ],
    });
    service = TestBed.inject({{ServiceClass}});
    httpMock = TestBed.inject(HttpTestingController);
  });

  afterEach(() => {
    httpMock.verify();
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });

  // Agente: gerar um it() por método público encontrado no .service.ts
  // Exemplos padrão abaixo — adaptar nomes de métodos e parâmetros:

  it('should GET with pagination params on getAll', () => {
    service.getAll({ page: 1, pageSize: 10, q: '' }).subscribe();
    const req = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}') && r.method === 'GET'
    );
    expect(req.request.params.get('page')).toBe('1');
    expect(req.request.params.get('pageSize')).toBe('10');
    req.flush({ items: [], hasNext: false });
  });

  it('should GET by id on getById', () => {
    service.getById('001').subscribe();
    const req = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}/001') && r.method === 'GET'
    );
    req.flush({});
  });

  it('should POST on create', () => {
    const mockPayload = {} as any;
    service.create(mockPayload).subscribe();
    const req = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}') && r.method === 'POST'
    );
    expect(req.request.body).toEqual(mockPayload);
    req.flush({});
  });

  it('should PUT on update', () => {
    const mockPayload = {} as any;
    service.update('001', mockPayload).subscribe();
    const req = httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}/001') && r.method === 'PUT'
    );
    expect(req.request.body).toEqual(mockPayload);
    req.flush({});
  });

  it('should DELETE on delete', () => {
    service.delete('001').subscribe();
    httpMock.expectOne(r =>
      r.url.includes('{{apiPath}}/001') && r.method === 'DELETE'
    ).flush(null, { status: 204, statusText: 'No Content' });
  });
});
```

## Nota para o agente

- Para service: substituir os nomes de método (`getAll`, `getById`, `create`, `update`, `delete`) pelos métodos reais encontrados no `.service.ts`
- Se o service tiver métodos adicionais (ex: `executarAcao`, `baixarTitulo`), adicionar um `it()` por método
- Para `executarAcao` do TitulosService: verificar método POST e payload `{ id }` ou `{ ids }`
```

- [ ] **Step 2: Atualizar `commands/generate.md`**

Localizar a linha exata no `generate.md`:
```
> **Auditoria periódica:** Use `/poui-specialist:quality` após gerar para verificar se os componentes atendem os 4 critérios de qualidade do plugin (OnPush, loading state, error handling, cleanup de observables) e se as rotas usam lazy loading.
```

Adicionar imediatamente após (nova linha):
```markdown
> **Geração de testes:** Use `/poui-specialist:test <ComponentClass> --module <module>` para gerar testes unitários Karma + Jasmine completos (smoke, HTTP, router, modais) para o componente gerado.
```

- [ ] **Step 3: Validar — invocar `/test` no TitulosService**

Invocar:
```
/poui-specialist:test TitulosService --module financeiro
```

O agente deve detectar `@Injectable` → família `other/service` e gerar spec cobrindo `getAll` (GET com params `page`, `pageSize`, `q`) e `executarAcao` (POST com payload `{ id }` ou `{ ids }`).

```powershell
cd "C:\TOTVS\Projetos\Claude\poui-specialist\Teste_poui"
ng test --include="src/app/financeiro/titulos.service.spec.ts" --watch=false
```

Resultado esperado: todos os specs passando.

- [ ] **Step 4: Commitar tudo e push**

```bash
git add skills/poui-test/templates-test-other.md commands/generate.md
git add Teste_poui/src/app/financeiro/titulos.service.spec.ts
git commit -m "feat(template): adicionar templates-test-other (dashboard/service) e referenciar /test no generate.md"
git push origin master
```

Resultado esperado: `master -> master` sem erros.
