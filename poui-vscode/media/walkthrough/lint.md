## Lint de Componentes

Varre o projeto atrás de anti-padrões conhecidos do PO-UI (grátis — não usa motor de IA).

```
Ctrl+Shift+P → "PO-UI: Lint de Componentes"
```

Exemplo de saída:

```
⚠ home-list.component.ts:42 — po-table sem [striped], considere adicionar
✓ home-form.component.ts — nenhum problema encontrado
```

[▶ Rodar agora](command:poui.lint)
